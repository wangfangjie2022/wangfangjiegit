
输入为 ModelC导航模块所用的传感器输入，
包括：两个IMU（'MPU6500', 'ICM20602' ），磁力计（'HMC5x83' ），气压计（'MS5611' ），GPS（'uBlox_PVT' ）
          视觉（'Vision' ），电调（'ESC' ），电池（'Battery' ），RTK（'RTK' ）

（预计）基站中实际安装的传感器为：IMU，磁力计，GPS
则其余传感器填 0 即可。


输出为：融合出的基站位姿，以及一些中间量。
包括：欧拉角，位置（LLH）等
